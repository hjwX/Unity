装箱：将值类型转换成引用类型
拆箱：将引用类型转换成值类型

装箱：
	1.首先要为新生成的引用对象在托管堆上分配内存大小
	2.把值类型的数据拷贝到引用对象上
	3.返回引用对象在托管堆上的地址，这个地址就是唯一指向引用对象的引用了
装箱过程要经过分配内存，拷贝数据这两个操作，这两个操作都是比较影响性能的操作
所以尽量减少装箱操作

拆箱：
	1.首先获取到值类型那部分数据在托管堆上的地址，严格意义上的拆箱，不会影响性能
	2.将堆上的数据拷贝到值类型上，会影响性能


